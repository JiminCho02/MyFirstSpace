name = "myfirstspace"
pages_build_output_dir = "out"

# Note: Cloudflare Pages may still require the Build Command to be set in the Dashboard
# if it doesn't automatically pick up 'npm run build' from your package.json.
